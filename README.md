# BFF Template

A template for GraphQL + NodeJS + Typescript + Apollo Server

To start the server
`ts-node index.ts`

Sample query at `http://localhost:8082/`

```
{
  books {
    title
    author
  }
}
```
